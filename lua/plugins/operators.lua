return {
  "echasnovski/mini.operators",
  -- cond = not vim.g.vscode,
  opts = {
    multiply = {
      prefix = "<leader>d",

      -- Function which can modify text before multiplying
      func = nil,
    },
  },
}
