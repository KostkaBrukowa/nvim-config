--[[ local tint = safe_require("tint") ]]
--[[]]
--[[ if not tint then ]]
--[[ 	return ]]
--[[ end ]]
--[[]]
--[[ tint.setup({ ]]
--[[ 	window_ignore_function = function(winid) ]]
--[[ 		local bufid = vim.api.nvim_win_get_buf(winid) ]]
--[[ 		local buftype = vim.api.nvim_buf_get_option(bufid, "buftype") ]]
--[[ 		local floating = vim.api.nvim_win_get_config(winid).relative ~= "" ]]
--[[]]
--[[ 		local currentBuffer = string.lower(vim.api.nvim_buf_get_name(bufid)) ]]
--[[ 		local is_tree_focused = string.find(currentBuffer, "nvimtree") ~= nil ]]
--[[]]
--[[ 		-- Do not tint `terminal` or floating windows, tint everything else ]]
--[[ 		return buftype == "terminal" or is_tree_focused or floating ]]
--[[ 	end, ]]
--[[ }) ]]
