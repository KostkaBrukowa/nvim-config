require("aerial").setup({
  manage_folds = true,
  show_guides = true,
  layout = {
    min_width = 45,
    default_direction = "left",
    manage_folds = true,
    link_folds_to_tree = false,
    win_opts = {
      number = true,
      relativenumber = true,
    },
  },
})
